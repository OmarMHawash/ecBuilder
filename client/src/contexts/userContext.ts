// import { createContext, useContext } from 'react'
// import type { userType, userConxtextType } from '../types/user'
// import type { builderType } from '../types/builder'

// export enum user {
//   name = 'lorem',
// }
// export const userContext = createContext<userConxtextType>({
//   user: { name: user.name },
//   builder: {
//     config: { c1: builder.config },
//     components: [],
//   },
//   setUser: (user: userType) => {},
//   setBuilder: (builder: builderType) => {},
// })

// export const useUser = (): userType => useContext(userContext)

export {}
